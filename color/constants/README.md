# constants

Purpose
- Immutable matrices, white points, and standard primaries.

Naming
- Use descriptive, lowercase names (snake_case) for constants.
- Group related constants in files like matrices.py, illuminants.py.

Entry points
- Prefer imports via color_agent.constants.<module>.
