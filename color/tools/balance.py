"""Brightness and luminance balancing helpers."""

from __future__ import annotations

from dataclasses import dataclass


@dataclass
class BrightnessBalancingToolkit:
    """Placeholder entry point for future luminance balancing strategies."""

    def balance(self, *args, **kwargs):
        raise NotImplementedError("Brightness balancing is not wired yet.")
