# tools

Purpose
- High-level workflows and toolkits for agent use.

Naming
- Toolkit classes end with Toolkit.
- Module names are short lowercase words (e.g. deltae).
- For legacy names with underscores, add a thin alias module.

Entry points
- Use color_agent.tools.<module> for a toolkit.
- Use color_agent.tools for the common toolkit exports.
